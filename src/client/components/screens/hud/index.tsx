import Roact from "@rbxts/roact";
import Layer from "client/components/ui/layer";

function HUD() {
    return (
        <Layer display_order={0}>
            
        </Layer>
    );
}

export default HUD;